package main

proc sender(b chan<- int) {
	b <- 42
}

func main() {
	a := make(chan int)
	b := make(chan int)
	c := make(chan int)
	chans := []chan int{a, b, c}
	par {
		sender(b)
		seq {
			i, v := altN(chans)
			println(i, v)
		}
	}
}
